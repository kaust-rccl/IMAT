function communicatingSubmitFcn(cluster, job, environmentProperties)

communicatingSubmitFcn = @IntegrationScripts.common.communicatingSubmitFcn;
communicatingSubmitFcn(cluster, job, environmentProperties);
